<Page x:Class="ModTogetherMHW.RecoveryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="RecoveryPage">
    <Grid Height="{Binding ViewportHeight, RelativeSource={RelativeSource AncestorType=ScrollViewer}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <!-- Panels Layout -->
        <Grid Margin="16" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Library -->
            <ui:Card Grid.Column="0" Padding="12" Margin="0,0,0,0" VerticalAlignment="Stretch" VerticalContentAlignment="Top">
                <Grid VerticalAlignment="Stretch">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12" Grid.Row="0">
                        <TextBlock x:Name="LblTitle" Text="Recycled Mods Library (.recycle_mods)" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                    </StackPanel>

                    <ui:TextBox x:Name="TxtSearch" PlaceholderText="Search mods..." TextChanged="TxtSearch_TextChanged" Grid.Row="1" Margin="0,0,0,8" />

                    <ListBox x:Name="ListMods" SelectionChanged="ListMods_SelectionChanged" Grid.Row="2" Background="{DynamicResource ControlFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1" Padding="0,0,0,0" VerticalAlignment="Stretch" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" VirtualizingStackPanel.IsVirtualizing="True" VirtualizingStackPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True" Grid.IsSharedSizeScope="True">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Margin" Value="0,0,0,2" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="4,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="DateCol" />
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="SizeCol" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal" Grid.Column="0" Margin="0,0,8,0">
                                        <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,6,0" />
                                        <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Foreground="{DynamicResource TextFillColorPrimaryBrush}" ToolTip="{Binding Filename}" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding DateModified}" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,12,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="11" />
                                    <TextBlock Text="{Binding Size}" Grid.Column="2" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="11" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </ui:Card>
        </Grid>

        <!-- Tools Card (Floating) -->
        <Border Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="16,0,16,16" Background="#26000000" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.5" ShadowDepth="4" Direction="270" />
            </Border.Effect>
            <WrapPanel Orientation="Horizontal" Margin="12,12,4,4" HorizontalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <ui:Button x:Name="BtnCheckAll" Content="Check All" Icon="{ui:SymbolIcon Checkmark24}" Click="BtnCheckAll_Click" Margin="0,0,8,8" />
                    <ui:Button x:Name="BtnUncheckAll" Content="Uncheck All" Icon="{ui:SymbolIcon Dismiss24}" Click="BtnUncheckAll_Click" Margin="0,0,16,8" />
                    <Rectangle Width="1" Fill="{DynamicResource CardStrokeColorDefaultBrush}" Margin="0,0,16,8" />
                </StackPanel>
                
                <StackPanel Orientation="Horizontal">
                    <ui:Button x:Name="BtnRestoreChecked" Content="Restore Checked" Icon="{ui:SymbolIcon ArrowUndo24}" Appearance="Primary" Click="BtnRestoreChecked_Click" Margin="0,0,8,8" />
                    <ui:Button x:Name="BtnDeleteChecked" Content="Delete Checked Permanently" Icon="{ui:SymbolIcon Delete24}" Appearance="Danger" Click="BtnDeleteChecked_Click" Margin="0,0,16,8" />
                    <Rectangle Width="1" Fill="{DynamicResource CardStrokeColorDefaultBrush}" Margin="0,0,16,8" />
                </StackPanel>
                
                <StackPanel Orientation="Horizontal">
                    <ui:Button x:Name="BtnRestore" Content="Restore" Icon="{ui:SymbolIcon ArrowUndo24}" Click="BtnRestore_Click" IsEnabled="False" Margin="0,0,8,8" />
                    <ui:Button x:Name="BtnDelete" Content="Delete Permanently" Icon="{ui:SymbolIcon Delete24}" Click="BtnDelete_Click" IsEnabled="False" Margin="0,0,8,8" />
                </StackPanel>
            </WrapPanel>
        </Border>
    </Grid>
</Page>

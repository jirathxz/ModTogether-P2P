<Page x:Class="ModTogether.Plugins.MHW.ManagerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      ScrollViewer.VerticalScrollBarVisibility="Disabled"
      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
      Title="ManagerPage">
    <Page.Resources>
        <ContextMenu x:Key="ModItemContextMenu">
            <MenuItem Header="Open Location" Click="MenuItem_OpenLocation_Click">
                <MenuItem.Icon>
                    <ui:SymbolIcon Symbol="FolderOpen24" />
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Backup Mod" Click="MenuItem_Backup_Click">
                <MenuItem.Icon>
                    <ui:SymbolIcon Symbol="Save24" />
                </MenuItem.Icon>
            </MenuItem>
            <Separator />
            <MenuItem Header="Delete Mod" Click="MenuItem_Delete_Click">
                <MenuItem.Icon>
                    <ui:SymbolIcon Symbol="Delete24" Foreground="Red" />
                </MenuItem.Icon>
            </MenuItem>
        </ContextMenu>
    </Page.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- ============================== -->
        <!-- TOP TOOLBAR: Global Mod Actions -->
        <!-- ============================== -->
        <Border Grid.Row="0" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="14,10" Background="{DynamicResource ControlFillColorDefaultBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Page Header Title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,16,0">
                    <ui:SymbolIcon Symbol="Games24" FontSize="20" Foreground="{DynamicResource SystemAccentColorPrimaryBrush}" Margin="0,0,8,0" VerticalAlignment="Center" />
                    <TextBlock Text="MHW Mod Manager" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />
                </StackPanel>

                <!-- Global Action Buttons (Center / Wrap) -->
                <WrapPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <ui:Button Content="Import Mod" Icon="{ui:SymbolIcon FolderAdd24}" Click="BtnImport_Click" Appearance="Primary" Margin="0,0,6,0" Padding="10,4" FontSize="12" />
                    <ui:Button Content="Refresh" Icon="{ui:SymbolIcon ArrowSync24}" Click="BtnRefresh_Click" Margin="0,0,6,0" Padding="10,4" FontSize="12" />
                    <ui:Button Content="Open Folder" Icon="{ui:SymbolIcon Folder24}" Click="BtnOpenFolder_Click" Margin="0,0,12,0" Padding="10,4" FontSize="12" />

                    <Border Width="1" Height="20" Background="{DynamicResource CardStrokeColorDefaultBrush}" Margin="0,0,12,0" VerticalAlignment="Center" />

                    <ui:Button Content="Validate" Icon="{ui:SymbolIcon Document24}" Click="BtnValidate_Click" Margin="0,0,6,0" Padding="10,4" FontSize="12" />
                    <ui:Button Content="Backup" Icon="{ui:SymbolIcon Save24}" Click="BtnBackup_Click" Margin="0,0,12,0" Padding="10,4" FontSize="12" />

                    <Border Width="1" Height="20" Background="{DynamicResource CardStrokeColorDefaultBrush}" Margin="0,0,12,0" VerticalAlignment="Center" />

                    <ui:Button Content="Install Checked" Icon="{ui:SymbolIcon Add24}" Click="BtnInstallChecked_Click" Appearance="Success" Margin="0,0,6,0" Padding="10,4" FontSize="12" />
                    <ui:Button Content="Uninstall Checked" Icon="{ui:SymbolIcon Subtract24}" Click="BtnUninstallChecked_Click" Margin="0,0,6,0" Padding="10,4" FontSize="12" />
                    <ui:Button Content="Delete Checked" Icon="{ui:SymbolIcon Delete24}" Click="BtnDeleteChecked_Click" Appearance="Danger" Margin="0,0,6,0" Padding="10,4" FontSize="12" />
                </WrapPanel>

                <!-- Selection Helpers -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <ui:Button Content="Check All" Icon="{ui:SymbolIcon Checkmark24}" Click="BtnCheckAll_Click" Margin="0,0,6,0" Padding="8,4" FontSize="11" />
                    <ui:Button Content="Uncheck All" Icon="{ui:SymbolIcon Dismiss24}" Click="BtnUncheckAll_Click" Padding="8,4" FontSize="11" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- ============================== -->
        <!-- MAIN WORKSPACE: Master-Detail   -->
        <!-- ============================== -->
        <Grid Grid.Row="1" Margin="14,14,14,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*" MinWidth="360" /> <!-- Mod Library (Left) -->
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" MinWidth="340" />    <!-- Inspector & File Tree (Right) -->
            </Grid.ColumnDefinitions>

            <!-- ============================== -->
            <!-- LEFT PANEL: Mod Library List   -->
            <!-- ============================== -->
            <ui:Card Grid.Column="0" Padding="0" VerticalAlignment="Stretch" VerticalContentAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Header & Sort -->
                    <Border Grid.Row="0" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="12,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="0">
                                <ui:SymbolIcon Symbol="FolderOpen24" FontSize="16" Foreground="{DynamicResource SystemAccentColorPrimaryBrush}" Margin="0,0,6,0" />
                                <TextBlock Text="Mod Library" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                            </StackPanel>

                            <ComboBox x:Name="ComboSort" Grid.Column="1" Width="120" SelectionChanged="ComboSort_SelectionChanged" SelectedIndex="0" MinHeight="28" FontSize="11">
                                <ComboBoxItem Content="Sort: Name" />
                                <ComboBoxItem Content="Sort: Date" />
                                <ComboBoxItem Content="Sort: Size" />
                            </ComboBox>
                        </Grid>
                    </Border>

                    <!-- Search Bar -->
                    <Border Grid.Row="1" Padding="10,8" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Background="{DynamicResource ControlFillColorDefaultBrush}">
                        <ui:TextBox x:Name="TxtSearch" PlaceholderText="Search mods..." TextChanged="TxtSearch_TextChanged" Icon="{ui:SymbolIcon Search24}" MinHeight="32" FontSize="12" />
                    </Border>

                    <!-- Legend -->
                    <Border Grid.Row="2" Padding="10,6" Background="{DynamicResource ControlFillColorSecondaryBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Width="8" Height="8" Background="#FF27AE60" CornerRadius="4" Margin="0,0,4,0"/>
                            <TextBlock Text="Installed" FontSize="11" Margin="0,0,12,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            
                            <Border Width="8" Height="8" Background="#FF95A5A6" CornerRadius="4" Margin="0,0,4,0"/>
                            <TextBlock Text="Not Installed" FontSize="11" Margin="0,0,12,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            
                            <Border Width="8" Height="8" Background="#FFE74C3C" CornerRadius="4" Margin="0,0,4,0"/>
                            <TextBlock Text="Conflict" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                    </Border>

                    <!-- Mods ListView -->
                    <ListView Grid.Row="3" x:Name="ListMods" 
                              SelectionChanged="ListMods_SelectionChanged" 
                              Background="Transparent" 
                              BorderThickness="0" 
                              AlternationCount="2" 
                              VirtualizingPanel.ScrollUnit="Pixel"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              Padding="6,4">
                        <ListView.Resources>
                            <!-- Custom Dark Header Style -->
                            <Style TargetType="{x:Type GridViewColumnHeader}">
                                <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter Property="FontSize" Value="11" />
                                <Setter Property="Padding" Value="8,6" />
                                <Setter Property="BorderThickness" Value="0,0,0,1" />
                                <Setter Property="BorderBrush" Value="{DynamicResource CardStrokeColorDefaultBrush}" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" />
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.Resources>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="{x:Type ListViewItem}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Margin" Value="0,2" />
                                <Setter Property="Padding" Value="8,8" />
                                <Setter Property="Background" Value="{Binding BackgroundColor}" />
                                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type ListViewItem}">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                <GridViewRowPresenter Content="{TemplateBinding Content}" Columns="{TemplateBinding GridView.ColumnCollection}" />
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="ContextMenu" Value="{StaticResource ModItemContextMenu}" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.9" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource SystemAccentColorPrimaryBrush}" />
                                        <Setter Property="Foreground" Value="White" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Width="36">
                                    <GridViewColumn.HeaderTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="✓" FontWeight="Bold" FontSize="11" ToolTip="Check for Batch Actions" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                        </DataTemplate>
                                    </GridViewColumn.HeaderTemplate>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>

                                <GridViewColumn Width="170" Header="Mod Name">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="12" Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=Foreground}" ToolTip="{Binding Filename}" TextTrimming="CharacterEllipsis" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>

                                <GridViewColumn Width="65" Header="Size">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Size}" VerticalAlignment="Center" Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=Foreground}" FontSize="11" Opacity="0.8" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>

                                <GridViewColumn Width="85" Header="Date">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding DateModified}" VerticalAlignment="Center" Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=Foreground}" FontSize="11" Opacity="0.8" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>

                                <GridViewColumn Width="80" Header="Owner">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Owner}" VerticalAlignment="Center" Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=Foreground}" FontSize="11" Opacity="0.8" TextTrimming="CharacterEllipsis"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </ui:Card>

            <!-- ============================== -->
            <!-- RIGHT PANEL: Mod Inspector     -->
            <!-- ============================== -->
            <ui:Card Grid.Column="2" Padding="0" VerticalAlignment="Stretch" VerticalContentAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Inspector Header & Actions -->
                    <Border Grid.Row="0" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="14,12" Background="{DynamicResource ControlFillColorDefaultBrush}">
                        <StackPanel>
                            <TextBlock x:Name="LblModInfo" Text="Select a mod from the list to inspect." TextWrapping="Wrap" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,12" />
                            
                            <!-- Contextual Actions -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <ui:Button x:Name="BtnInstall" Grid.Column="0" Click="BtnInstall_Click" Content="Install" Icon="{ui:SymbolIcon Add24}" Appearance="Primary" HorizontalAlignment="Stretch" IsEnabled="False" Margin="0,0,4,0" MinHeight="32" FontSize="12" />
                                <ui:Button x:Name="BtnUninstall" Grid.Column="1" Click="BtnUninstall_Click" Content="Uninstall" Icon="{ui:SymbolIcon Subtract24}" HorizontalAlignment="Stretch" IsEnabled="False" Margin="2,0,4,0" MinHeight="32" FontSize="12" />
                                <ui:Button x:Name="BtnDelete" Grid.Column="2" Click="BtnDelete_Click" Content="Delete" Icon="{ui:SymbolIcon Delete24}" Appearance="Danger" HorizontalAlignment="Stretch" IsEnabled="False" Margin="2,0,0,0" MinHeight="32" FontSize="12" />
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- File Tree Section Header -->
                    <Border Grid.Row="1" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="12,8" Background="{DynamicResource ControlFillColorSecondaryBrush}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="0">
                                <ui:SymbolIcon Symbol="Document24" FontSize="14" Foreground="{DynamicResource SystemAccentColorPrimaryBrush}" Margin="0,0,6,0" />
                                <TextBlock Text="Mod Files (nativePC)" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Grid.Column="1" VerticalAlignment="Center">
                                <ui:Button Content="Check All" Icon="{ui:SymbolIcon Checkmark24}" Click="BtnCheckAllTree_Click" Margin="0,0,4,0" Padding="6,2" FontSize="11" />
                                <ui:Button Content="Uncheck All" Icon="{ui:SymbolIcon Dismiss24}" Click="BtnUncheckAllTree_Click" Padding="6,2" FontSize="11" />
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Mod Files TreeView -->
                    <Grid Grid.Row="2" Margin="6,6,6,6">
                        <TreeView x:Name="_listModFiles" Background="Transparent" BorderThickness="0" VirtualizingStackPanel.IsVirtualizing="True" VirtualizingStackPanel.VirtualizationMode="Recycling">
                            <TreeView.ItemTemplate>
                                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                    <StackPanel Orientation="Horizontal" Margin="0,2">
                                        <CheckBox IsThreeState="True" IsChecked="{Binding IsChecked, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,6,0" />
                                        <ui:SymbolIcon Filled="True" Margin="0,0,6,0" VerticalAlignment="Center" FontSize="14">
                                            <ui:SymbolIcon.Style>
                                                <Style TargetType="ui:SymbolIcon">
                                                    <Setter Property="Symbol" Value="Document24"/>
                                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsDirectory}" Value="True">
                                                            <Setter Property="Symbol" Value="Folder24"/>
                                                            <Setter Property="Foreground" Value="{DynamicResource SystemAccentColorPrimaryBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ui:SymbolIcon.Style>
                                        </ui:SymbolIcon>
                                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontSize="12" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                    </StackPanel>
                                </HierarchicalDataTemplate>
                            </TreeView.ItemTemplate>
                            <TreeView.ItemContainerStyle>
                                <Style TargetType="TreeViewItem">
                                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                                    <Setter Property="Padding" Value="4,2"/>
                                </Style>
                            </TreeView.ItemContainerStyle>
                        </TreeView>
                        <ui:ProgressRing x:Name="LoadingRing" IsIndeterminate="True" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Width="40" Height="40" />
                    </Grid>
                </Grid>
            </ui:Card>
        </Grid>
    </Grid>
</Page>
